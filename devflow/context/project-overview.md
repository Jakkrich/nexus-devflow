# Project Overview & Source of Truth

> Living context artifact generated by `scripts/overview.ts` on 2026-09-16T08:11:01.707Z.

## 1. Project Purpose & Target Users

> โยนเนื้อหายังไม่ครบใน section นี้

## 2. Architecture & Directory Layout

- **Language / Framework**: [เช่น Node.js, TypeScript, Next.js / Express]
- **Database**: [เช่น SQLite, PostgreSQL]
- **Hosting / Deployment**: [เช่น Local CLI, Vercel, Render, Docker]
> โยนเนื้อหายังไม่ครบใน section นี้

## 3. Technology Stack & Tooling

- Runtime: Node.js (>=18.17)
- ภาษา: TypeScript
- Verified/Dev commands:
  - build: `ยังไม่กำหนด`
  - test: `npm --prefix packages/create-nexus-devflow test && npm run test:overview && npm run test:sandbox && npm run test:run-state && npm run test:tooling`
  - check: `npm run typecheck && tsx scripts/check-devflow.ts`
  - check:static: `tsx scripts/validate-framework.ts`

## 4. Source-of-Truth Constraints & Scope Gaps

- **Language / Framework**: [เช่น Node.js, TypeScript, Next.js / Express]
- **Database**: [เช่น SQLite, PostgreSQL]
- **Hosting / Deployment**: [เช่น Local CLI, Vercel, Render, Docker]

## 5. Upcoming Build Queue

- [ ] 1. **Core Schema & Data Models** - สร้าง Data Models, Schemas (Zod) และ Database Migrations เบื้องต้น
- [ ] 2. **Base Layout & Main UI Shell** - วางโครงสร้างหน้าจอหลัก, Navigation Shell และ Design Tokens
- [ ] 3. **Primary Feature Flow** - พัฒนาฟังก์ชันการทำงานหลัก พร้อม Input Form และ Validation
- [ ] 4. **Live Preview / Action Pipeline** - เชื่อมต่อ Input เข้าสู่ระบบประมวลผลและการแสดงผลแบบ Real-time
- [ ] 5. **Export / Output Engine** - พัฒนาระบบส่งออกข้อมูล เช่น Image Generation, PDF Download หรือ API Response
- [ ] 6. **Local Persistence & Settings** - บันทึกการตั้งค่าและประวัติการใช้งานลงใน Local Storage หรือ Database
- [ ] 6a. **Settings Panel** - UI สำหรับปรับแต่งค่าและการจัดเก็บ State
- [ ] 6b. **History & Recall** - แสดงรายการประวัติย้อนหลังและปุ่มเรียกข้อมูลกลับมาใช้
- [ ] 7. **Edge Cases & Input Polish** - จัดการ Loading States, Error Boundaries และ Responsive Layout
- [ ] 8. **Deployment Readiness & Production Verification** - ตั้งค่า Environment Config, ตรวจสอบ Production Build และ Health Check
- [ ] **Cloud Sync & User Accounts** - ระบบสมัครสมาชิกและซิงค์ข้อมูลบน Cloud

## 6. Shipped Capabilities Snapshot

- **Feature**: Sync Upstream AI Blueprint v1.7.0–v1.9.0
- **Fix**: Prevent unused devflow/runs from client install
- **Fix**: Align skill prompts and routing verification

## 7. Ideas & Backlog Pulse

- รวมไอเดียทั้งหมด: 0
- ไอเดียค้างอยู่: 0

## 8. Data Sources Read by Compiler

- โฟลเดอร์หลัก: assets, benchmarks, devflow, e2e, evals, packages, prototypes, scripts
- สภาพทั่วไป: ข้อมูลจริงถูกรูทผ่านไฟล์ใน `devflow/` และสคริปต์ช่วยอัปเดต

## 9. Verified Developer Commands

- `npm run check`
- `npm run check:static`
- `npm run test`
- `npm run test:package`
- `npm run overview -- --write`
