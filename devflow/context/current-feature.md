# Current Feature

> **Living Source of Truth**: Active Work Context (Single Living Spec Model)

_Nothing in progress._

To start a new task:
- For a new feature: `/feature <name>` (or `$feature <name>`)
- For a bug fix: `/fix <name>` (or `$fix <name>`)
- For a safe reversal: `/rollback <feature-id>` (or `$rollback <feature-id>`)
- For pre-delivery exploration: `/discovery <topic>` (or `$discovery <topic>`)
