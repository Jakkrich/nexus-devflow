# .agent Docs

This directory is reserved for bundle-local documentation that supports the `.agent` package structure.

In DevFlow 2.0, the primary human-readable documentation still lives under the repository-level `docs/` directory, while this folder exists to satisfy bundle structure requirements and to host any future agent-bundle-local notes if needed.
