{
  "skill": "package-json-generator",
  "description": "\"[Devflow] Generates or updates a package.json file with standardized npm scripts for the project. Automatically migrates tasks from .vscode/tasks.json if present. Use when initializing npm scripts, migrating from VS Code tasks, or setting up a centralized task runner.\"",
  "positive": [
    "run package-json-generator skill",
    "invoke /package-json-generator command",
    "use package-json-generator for devflow generates updates package json file with standardized",
    "apply package-json-generator guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
