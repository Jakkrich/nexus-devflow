{
  "skill": "domain-modeling",
  "description": "\"[Devflow] Build and sharpen a project's domain model. Use when the user wants to pin down domain terminology or a ubiquitous language, record an architectural decision, or when another skill needs to maintain the domain model.\"",
  "positive": [
    "run domain-modeling skill",
    "invoke /domain-modeling command",
    "use domain-modeling for devflow build sharpen project domain model when user",
    "apply domain-modeling guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
