{
  "skill": "ci",
  "description": "\"[Devflow] Set up automated GitHub Actions CI workflow (.github/workflows/verify.yml) aligned with project verify command.\"",
  "positive": [
    "run ci skill",
    "invoke /ci command",
    "use ci for devflow automated github actions workflow github workflows verify",
    "apply ci guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
