{
  "skill": "ci",
  "description": "\"[Devflow] CI/CD pipeline automation and GitHub Actions setup (.github/workflows/verify.yml). Automates quality gates, typechecks, tests, and build checks.\"",
  "positive": [
    "run ci skill",
    "invoke /ci command",
    "use ci for devflow pipeline automation github actions setup github workflows",
    "apply ci guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
