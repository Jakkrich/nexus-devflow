{
  "skill": "20-spec",
  "description": "\"[Devflow] Spec stage in DevFlow 2.0 - write the formal markdown-first specification from a stable definition.\"",
  "positive": [
    "run 20-spec skill",
    "invoke /20-spec command",
    "use 20-spec for devflow spec stage devflow write formal markdown first",
    "apply 20-spec guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
