{
  "skill": "silent-failure-audit",
  "description": "Audits error handling, logging, fallbacks, and observability for silent failures. Use after adding catch blocks, fallback logic, retry handling, optional chaining, or user-facing failure paths.",
  "positive": [
    "run silent-failure-audit skill",
    "invoke /silent-failure-audit command",
    "use silent-failure-audit for audits error handling logging fallbacks observability silent failures",
    "apply silent-failure-audit guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
