{
  "skill": "onboard",
  "description": "\"[devflow] Onboard a fresh or early scaffold after DevFlow is overlaid by tuning commands, standards, adapters, visibility, review cadence, and context loading. Use for /onboard or fresh installation setup.\"",
  "positive": [
    "run onboard skill",
    "invoke /onboard command",
    "use onboard for devflow onboard fresh early scaffold after devflow overlaid",
    "apply onboard guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
