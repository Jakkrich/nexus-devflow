{
  "skill": "incremental-implementation",
  "description": "Delivers changes incrementally. Use when implementing any feature or change that touches more than one file. Use when you're about to write a large amount of code at once, or when a task feels too big to land in one step.",
  "positive": [
    "run incremental-implementation skill",
    "invoke /incremental-implementation command",
    "use incremental-implementation for delivers changes incrementally when implementing feature change that",
    "apply incremental-implementation guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
