{
  "skill": "agent",
  "description": "\"[Devflow] Invoke specialist persona or role-based agent on a target file, folder, stage artifact, or concern.\"",
  "positive": [
    "run agent skill",
    "invoke /agent command",
    "use agent for devflow invoke specialist persona role based agent target",
    "apply agent guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
