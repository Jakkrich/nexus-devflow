{
  "skill": "overview",
  "description": "\"[devflow] Distill user-owned planning docs into `devflow/context/project-overview.md` using a deterministic compiler. The overview is the living source of truth that agents read every session.\"",
  "positive": [
    "run overview skill",
    "invoke /overview command",
    "use overview for devflow distill user owned planning docs into devflow",
    "apply overview guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
