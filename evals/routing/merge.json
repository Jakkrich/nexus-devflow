{
  "skill": "merge",
  "description": "PR Merge (Git Orchestration) - Safely merge the current feature branch into the base branch and clean up.",
  "positive": [
    "run merge skill",
    "invoke /merge command",
    "use merge for merge orchestration safely merge current feature branch into",
    "apply merge guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
