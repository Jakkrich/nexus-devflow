{
  "skill": "merge",
  "description": "\"[Devflow] Safe PR branch merge into base branch, cleanup, and release readiness check.\"",
  "positive": [
    "run merge skill",
    "invoke /merge command",
    "use merge for devflow safe branch merge into base branch cleanup",
    "apply merge guidance"
  ],
  "negative": [
    "unrelated random task query",
    "general generic string prompt"
  ]
}
